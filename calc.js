//display items
function display(num){
    result.value+=num
    }
    
    function allclear(){
        result.value=''
    }
    function equall(){
        result.value = eval(result.value)
    }
    //function back(){
       // result.value=result.value.slice(0,-1)
    //}